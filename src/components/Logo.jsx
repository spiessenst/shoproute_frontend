import logo from "../public/img/logo.svg";

const Logo = () => {
  return (
    <div className="logo">
      <img src={logo} alt="ShopRoute" className="logo__brand" />
    </div>
  );
};

export default Logo;
